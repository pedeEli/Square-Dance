{
  "name": "temporary",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --host etest",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^4.5.4",
    "vite": "^2.8.0"
  }
}
